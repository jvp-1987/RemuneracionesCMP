const axios = require('axios');
(async () => {
  try {
    const pIds = '9999';
    const baseUrl = 'http://localhost:3001'; // Need to know backend url, let's use the local API if we can, or just mock what the backend does.
  } catch (e) {
    console.error(e);
  }
})();
