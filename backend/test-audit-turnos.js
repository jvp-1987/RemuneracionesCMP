const axios = require('axios');

async function run() {
  try {
    // 1. Get an existing turno
    // We don't have the real API URL here, but maybe we can query DB directly
  } catch (err) {
    console.error(err);
  }
}
run();
